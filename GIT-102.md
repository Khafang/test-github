# Git 102 - Rebase, Merge, Reset, Cherry-Pick & Squash

บทนี้เหมาะสำหรับคนที่ใช้ Git พื้นฐานได้แล้ว และอยากเข้าใจเครื่องมือที่ทรงพลัง (และอันตราย) กว่าเดิม

---

## สารบัญ

1. [Merge - การรวม branch](#merge---การรวม-branch)
2. [Rebase - การย้ายฐาน branch](#rebase---การย้ายฐาน-branch)
3. [Merge vs Rebase เมื่อไหร่ใช้อะไร](#merge-vs-rebase-เมื่อไหร่ใช้อะไร)
4. [Reset - การย้อนกลับ commit](#reset---การย้อนกลับ-commit)
5. [Cherry-Pick - หยิบ commit ที่ต้องการ](#cherry-pick---หยิบ-commit-ที่ต้องการ)
6. [Squash - รวม commit ให้เป็นก้อนเดียว](#squash---รวม-commit-ให้เป็นก้อนเดียว)
7. [ข้อควรระวังและ Bad Practices](#ข้อควรระวังและ-bad-practices)

---

## Merge - การรวม branch

Merge คือการนำ commit จาก branch หนึ่งมารวมเข้ากับ branch ปัจจุบัน

### Fast-Forward Merge

เกิดขึ้นเมื่อ branch ปัจจุบันไม่มี commit ใหม่ตั้งแต่แยก branch ออกไป Git จะเลื่อน pointer ไปข้างหน้าเฉยๆ

```
ก่อน merge:
main:     A --- B
                 \
feature:          C --- D

หลัง merge (fast-forward):
main:     A --- B --- C --- D
```

```bash
git checkout main
git merge feature
# ได้ fast-forward เพราะ main ไม่มี commit ใหม่
```

### 3-Way Merge (Merge Commit)

เกิดขึ้นเมื่อทั้งสอง branch มี commit ใหม่ทั้งคู่ Git จะสร้าง **merge commit** ใหม่ขึ้นมา

```
ก่อน merge:
main:     A --- B --- E
                 \
feature:          C --- D

หลัง merge (3-way):
main:     A --- B --- E --- M  (M = merge commit)
                 \         /
feature:          C --- D
```

```bash
git checkout main
git merge feature
# สร้าง merge commit อัตโนมัติ
```

### บังคับสร้าง Merge Commit (--no-ff)

บางทีมบังคับสร้าง merge commit เสมอ เพื่อให้เห็นประวัติชัดเจนว่า feature นี้มาจาก branch ไหน

```bash
git merge --no-ff feature/login-page
# สร้าง merge commit เสมอ แม้จะ fast-forward ได้
```

---

## Rebase - การย้ายฐาน branch

Rebase คือการ **ย้าย commit ทั้งหมดของ branch เราไปต่อท้าย branch อื่น** เหมือนหยิบงานเราไปวางใหม่บน code ล่าสุด

```
ก่อน rebase:
main:     A --- B --- E
                 \
feature:          C --- D

หลัง rebase:
main:     A --- B --- E
                       \
feature:                C' --- D'
(C' D' คือ commit เดิมที่ถูกสร้างใหม่)
```

### วิธีใช้

```bash
# อยู่ที่ feature branch
git checkout feature/login-page

# ย้ายฐานไปต่อท้าย main
git rebase main
```

### Rebase แบบ Interactive

ใช้จัดการ commit ก่อน merge เช่น รวม commit, แก้ข้อความ, เรียงลำดับใหม่

```bash
git rebase -i HEAD~3
# จัดการ 3 commit ล่าสุด
```

จะเปิด editor ขึ้นมาให้เลือก action:

```
pick abc1234 feat: add login form
pick def5678 fix: typo in login
pick ghi9012 feat: add validation

# คำสั่งที่ใช้ได้:
# p, pick   = ใช้ commit นี้ (ไม่เปลี่ยนอะไร)
# r, reword = ใช้ commit นี้ แต่แก้ข้อความ
# e, edit   = ใช้ commit นี้ แต่หยุดให้แก้ไข
# s, squash = รวมเข้ากับ commit ก่อนหน้า (เก็บข้อความ)
# f, fixup  = รวมเข้ากับ commit ก่อนหน้า (ทิ้งข้อความ)
# d, drop   = ลบ commit นี้ทิ้ง
```

### แก้ Conflict ระหว่าง Rebase

```bash
# 1. เจอ conflict → แก้ไฟล์ที่ conflict
# 2. add ไฟล์ที่แก้แล้ว
git add <ไฟล์ที่-conflict>

# 3. ทำ rebase ต่อ
git rebase --continue

# ถ้าอยากยกเลิก rebase ทั้งหมด (กลับไปเหมือนเดิม)
git rebase --abort
```

---

## Merge vs Rebase เมื่อไหร่ใช้อะไร

| | Merge | Rebase |
|---|-------|--------|
| **ประวัติ** | เก็บ branch history ครบ | ประวัติเป็นเส้นตรง สะอาด |
| **Merge commit** | มี | ไม่มี |
| **ปลอดภัย** | ปลอดภัยกว่า ไม่แก้ประวัติ | เขียนประวัติใหม่ (rewrite history) |
| **เหมาะกับ** | รวม branch เข้า main | อัปเดต feature branch ให้ทันสมัย |
| **Conflict** | แก้ครั้งเดียว | อาจต้องแก้ทีละ commit |

### กฎทอง (Golden Rule of Rebase)

> **อย่า rebase branch ที่คนอื่นใช้ร่วมด้วย!**
>
> Rebase เขียนประวัติใหม่ ถ้า branch นั้นมีคนอื่น pull ไปแล้ว จะทำให้ประวัติ
> ไม่ตรงกัน เกิดปัญหาวุ่นวาย

```
ปลอดภัย:    rebase feature branch ส่วนตัว ✅
อันตราย:    rebase main หรือ branch ที่แชร์กับคนอื่น ❌
```

### Workflow ที่แนะนำ

```bash
# อัปเดต feature branch ให้ทันกับ main → ใช้ rebase
git checkout feature/my-work
git rebase main

# รวม feature branch เข้า main → ใช้ merge
git checkout main
git merge feature/my-work
```

---

## Reset - การย้อนกลับ commit

`git reset` ย้าย HEAD (และ branch pointer) กลับไปยัง commit ที่ระบุ มี 3 โหมด:

```
commit history:  A --- B --- C --- D (HEAD)
                                    ↑
                              จะ reset กลับไป C
```

### --soft

ย้าย HEAD กลับ แต่ **เก็บทุกอย่างไว้ใน staging area** (พร้อม commit ใหม่ได้เลย)

```bash
git reset --soft HEAD~1
# ย้อน 1 commit แต่ไฟล์ที่แก้ยังอยู่ใน staging
# เหมือน "undo commit" แต่ยังไม่ undo add
```

```
สถานะหลัง --soft:
Working Directory: ไม่เปลี่ยน
Staging Area:      มีการเปลี่ยนแปลงของ commit D อยู่
HEAD:              ชี้ไปที่ C
```

### --mixed (ค่าเริ่มต้น)

ย้าย HEAD กลับ และ **เอาไฟล์ออกจาก staging** แต่ไฟล์ที่แก้ยังอยู่ใน working directory

```bash
git reset HEAD~1
# หรือ
git reset --mixed HEAD~1
# ย้อน 1 commit ไฟล์ที่แก้ยังอยู่ แต่ต้อง add ใหม่
```

```
สถานะหลัง --mixed:
Working Directory: มีการเปลี่ยนแปลงของ commit D อยู่
Staging Area:      ว่างเปล่า
HEAD:              ชี้ไปที่ C
```

### --hard

ย้าย HEAD กลับ และ **ลบทุกอย่าง** ทั้ง staging และ working directory กลับไปเหมือน commit นั้นเลย

```bash
git reset --hard HEAD~1
# ⚠️ อันตราย! ลบการเปลี่ยนแปลงทั้งหมดหายไปเลย
```

```
สถานะหลัง --hard:
Working Directory: กลับไปเหมือน commit C
Staging Area:      ว่างเปล่า
HEAD:              ชี้ไปที่ C
```

### เปรียบเทียบ Reset ทั้ง 3 โหมด

| โหมด | HEAD | Staging Area | Working Directory | ระดับอันตราย |
|-------|------|-------------|-------------------|-------------|
| `--soft` | ย้อนกลับ | ยังอยู่ | ยังอยู่ | ต่ำ |
| `--mixed` | ย้อนกลับ | ถูกเคลียร์ | ยังอยู่ | กลาง |
| `--hard` | ย้อนกลับ | ถูกเคลียร์ | ถูกเคลียร์ | สูง |

### Reset vs Revert

```bash
# Reset = ลบ commit ออกจากประวัติ (เขียนประวัติใหม่)
git reset --hard HEAD~1       # commit หายไปเลย

# Revert = สร้าง commit ใหม่ที่ undo การเปลี่ยนแปลง (ประวัติยังอยู่)
git revert HEAD               # สร้าง commit ใหม่ที่ย้อนกลับ
```

> **ใช้ revert** กับ commit ที่ push ไปแล้ว
> **ใช้ reset** กับ commit ที่ยังอยู่ใน local เท่านั้น

---

## Cherry-Pick - หยิบ commit ที่ต้องการ

Cherry-pick คือการ **หยิบ commit เฉพาะตัว** จาก branch อื่นมาใส่ branch ปัจจุบัน โดยไม่ต้อง merge ทั้ง branch

```
ก่อน cherry-pick:
main:     A --- B --- E
                 \
feature:          C --- D --- F

สมมติต้องการเอาเฉพาะ commit D มาใส่ main:

หลัง cherry-pick:
main:     A --- B --- E --- D'
                 \
feature:          C --- D --- F
(D' คือ copy ของ D บน main)
```

### วิธีใช้

```bash
# 1. หา commit hash ที่ต้องการ
git log --oneline feature/payment
# abc1234 feat: add payment gateway
# def5678 fix: payment timeout
# ghi9012 feat: add cart total

# 2. cherry-pick commit ที่ต้องการ
git checkout main
git cherry-pick def5678
# หยิบเฉพาะ commit def5678 มาใส่ main
```

### Cherry-pick หลาย commit

```bash
# หยิบหลาย commit (ระบุทีละตัว)
git cherry-pick abc1234 def5678

# หยิบช่วง commit (ไม่รวมตัวแรก..รวมตัวสุดท้าย)
git cherry-pick abc1234..ghi9012
```

### แก้ Conflict ตอน Cherry-pick

```bash
# ถ้าเจอ conflict
# 1. แก้ไฟล์ที่ conflict
# 2. add ไฟล์ที่แก้แล้ว
git add <ไฟล์ที่-conflict>

# 3. ทำต่อ
git cherry-pick --continue

# ถ้าอยากยกเลิก
git cherry-pick --abort
```

### เมื่อไหร่ควรใช้ Cherry-pick

- ต้องการ **hotfix** ด่วน: หยิบ fix จาก develop มาใส่ main โดยไม่ merge ทั้ง branch
- อยากได้ **เฉพาะบาง commit** จาก branch ที่ยังทำไม่เสร็จ
- **backport** แก้ bug ไป branch เวอร์ชันเก่า

---

## Squash - รวม commit ให้เป็นก้อนเดียว

Squash คือการ **รวมหลาย commit เข้าด้วยกันเป็น commit เดียว** เพื่อให้ประวัติสะอาดก่อน merge

### ทำไมต้อง Squash?

```bash
# ก่อน squash (commit เยอะเกินไป ไม่เป็นระเบียบ):
abc1234 feat: add login form
def5678 fix: typo
ghi9012 fix: forgot semicolon
jkl3456 fix: css alignment
mno7890 feat: add validation

# หลัง squash (รวมเป็น commit เดียวที่สื่อความหมาย):
xyz9999 feat: add login page with form validation
```

### วิธีที่ 1: Squash ด้วย Interactive Rebase

```bash
# รวม 4 commit ล่าสุดเป็นก้อนเดียว
git rebase -i HEAD~4
```

จะเปิด editor ขึ้นมา:

```
pick abc1234 feat: add login form
pick def5678 fix: typo
pick ghi9012 fix: forgot semicolon
pick jkl3456 fix: css alignment
```

แก้เป็น:

```
pick abc1234 feat: add login form
squash def5678 fix: typo
squash ghi9012 fix: forgot semicolon
squash jkl3456 fix: css alignment
```

> `pick` ตัวแรก = commit หลัก
> `squash` ที่เหลือ = รวมเข้ากับ commit ที่อยู่ด้านบน

Git จะเปิด editor อีกทีให้เขียน commit message ใหม่

### วิธีที่ 2: Squash ตอน Merge

```bash
git checkout main
git merge --squash feature/login-page
git commit -m "feat: add login page with form validation"
```

> `--squash` จะรวมทุก commit จาก feature branch เป็นการเปลี่ยนแปลงก้อนเดียว
> แล้วเราเขียน commit message ใหม่เอง

### วิธีที่ 3: Soft Reset แล้ว Commit ใหม่

```bash
# สมมติอยากรวม 4 commit ล่าสุด
git reset --soft HEAD~4
git commit -m "feat: add login page with form validation"
```

> เทคนิคนี้ง่ายสุด แต่ใช้ได้แค่กับ commit ที่ยังไม่ push

---

## ข้อควรระวังและ Bad Practices

### 1. Force Push โดยไม่ระวัง

```bash
# ❌ อันตรายมาก - เขียนทับงานคนอื่นบน remote
git push --force

# ✅ ปลอดภัยกว่า - จะไม่ push ถ้ามีคนอื่น push มาก่อน
git push --force-with-lease
```

> `--force` บน shared branch = หายนะ
> ถ้าจำเป็นต้อง force push ให้ใช้ `--force-with-lease` เสมอ
> และ **แจ้งทีมก่อนทุกครั้ง**

### 2. Rebase branch ที่แชร์กับคนอื่น

```bash
# ❌ ห้ามทำ! rebase main หรือ develop ที่คนอื่นใช้ร่วม
git checkout main
git rebase feature/something   # ประวัติ main เปลี่ยน → ทุกคนพัง

# ✅ rebase เฉพาะ branch ส่วนตัว
git checkout feature/my-work
git rebase main                # อัปเดต branch ตัวเองให้ทัน main
```

### 3. Reset --hard โดยไม่ตรวจสอบก่อน

```bash
# ❌ ลบทุกอย่างหายเลย ไม่มี undo
git reset --hard HEAD~5

# ✅ ตรวจสอบก่อนว่าจะสูญเสียอะไรบ้าง
git log --oneline HEAD~5..HEAD    # ดูว่า commit ไหนจะหาย
git stash                         # เก็บงานที่ยังไม่ commit ไว้ก่อน
git reset --hard HEAD~5
```

> ถ้า reset --hard ไปแล้วอยากกู้คืน ลองใช้ `git reflog`
> ```bash
> git reflog                        # ดูประวัติทุก HEAD movement
> git reset --hard <reflog-hash>    # กู้คืนกลับไปจุดนั้น
> ```

### 4. Commit ไม่บ่อยพอ แล้ว Squash ทีเดียว

```bash
# ❌ ทำงาน 3 วัน ไม่ commit เลย แล้ว commit ทีเดียว
git add .
git commit -m "update everything"

# ✅ commit บ่อยๆ เป็นก้อนเล็กๆ แล้วค่อย squash ก่อน merge
git commit -m "feat: add login form UI"
git commit -m "feat: add form validation"
git commit -m "test: add login tests"
# แล้วค่อย squash ก่อน merge ถ้าต้องการ
```

### 5. Cherry-pick แทน Merge ทั้งหมด

```bash
# ❌ cherry-pick ทุก commit ทีละตัวแทนที่จะ merge
git cherry-pick abc1234
git cherry-pick def5678
git cherry-pick ghi9012
# ทำให้ได้ duplicate commit → สับสน → conflict ภายหลัง

# ✅ ถ้าต้องการทุก commit ใน branch ให้ merge แทน
git merge feature/login-page
```

### 6. ใช้ git reset กับ commit ที่ push ไปแล้ว

```bash
# ❌ reset commit ที่ push ไปแล้ว → ต้อง force push → คนอื่นพัง
git reset --hard HEAD~3
git push --force

# ✅ ใช้ revert สำหรับ commit ที่ push ไปแล้ว
git revert HEAD~2..HEAD     # สร้าง revert commit ย้อนกลับ
git push                    # push ปกติ ไม่ต้อง force
```

### 7. ไม่อ่าน Conflict ให้ดี แก้แบบมั่วๆ

```bash
# ❌ เจอ conflict แล้วเลือก "Accept All Current" หรือ "Accept All Incoming" โดยไม่ดู
# อาจทำให้โค้ดคนอื่นหายไป หรือ logic ผิด

# ✅ อ่าน conflict ทีละจุด เข้าใจว่าทั้งสองฝั่งทำอะไร แล้วค่อยตัดสินใจ
```

### 8. ลืมดู git status ก่อนทำอะไร

```bash
# ❌ ทำ rebase, merge, reset โดยไม่เช็คว่ามีงานค้างอยู่ไหม
git rebase main        # อาจมี uncommitted changes ที่หายไป

# ✅ เช็คก่อนเสมอ
git status             # ดูว่ามีงานค้างไหม
git stash              # เก็บงานค้างไว้ก่อน (ถ้ามี)
git rebase main
git stash pop          # เอางานค้างกลับมา
```

---

## สรุป Cheat Sheet

| คำสั่ง | ทำอะไร | อันตราย? |
|--------|--------|----------|
| `git merge <branch>` | รวม branch (เก็บประวัติครบ) | ปลอดภัย |
| `git merge --no-ff <branch>` | รวม branch (สร้าง merge commit เสมอ) | ปลอดภัย |
| `git merge --squash <branch>` | รวม branch เป็น commit เดียว | ปลอดภัย |
| `git rebase <branch>` | ย้ายฐาน branch | ⚠️ เขียนประวัติใหม่ |
| `git rebase -i HEAD~N` | จัดการ N commit ล่าสุด | ⚠️ เขียนประวัติใหม่ |
| `git reset --soft HEAD~N` | ย้อน N commit (เก็บใน staging) | ต่ำ |
| `git reset --mixed HEAD~N` | ย้อน N commit (เก็บใน working dir) | กลาง |
| `git reset --hard HEAD~N` | ย้อน N commit (ลบทุกอย่าง) | 🔴 สูงมาก |
| `git cherry-pick <hash>` | หยิบ commit เฉพาะตัว | ปลอดภัย |
| `git revert <hash>` | สร้าง commit ที่ undo | ปลอดภัย |
| `git push --force-with-lease` | force push แบบปลอดภัย | ⚠️ ใช้ด้วยความระวัง |

---

## กฎเหล็ก 5 ข้อ

1. **อย่า rewrite history ของ shared branch** - rebase, reset, amend เฉพาะ branch ส่วนตัว
2. **อย่า force push โดยไม่แจ้งทีม** - ถ้าจำเป็นใช้ `--force-with-lease`
3. **git status ก่อนทำทุกอย่าง** - เช็คสถานะเสมอ
4. **ใช้ revert แทน reset สำหรับ commit ที่ push แล้ว** - ไม่ต้อง force push
5. **เข้าใจก่อนใช้** - ถ้าไม่แน่ใจว่าคำสั่งทำอะไร อย่าเพิ่งรัน ลองใน branch ทดสอบก่อน
